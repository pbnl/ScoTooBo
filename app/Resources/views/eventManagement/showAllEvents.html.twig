{% extends "includes/base.html.twig" %}

{% block title %}{% trans %}menu.eventManagement.title{% endtrans %}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {% include 'includes/navbar.html.twig' %}
    <div class="container">
        <br><br>
        {% include 'includes/showFlashes.html.twig' %}
        <!--
        <h3>Such Optionen</h3>
        <div class="col-sm-6">
            {% if peopleSearchForm is defined %}
                {% form_theme peopleSearchForm 'bootstrap_3_horizontal_layout.html.twig' %}
                {{ form_start(peopleSearchForm) }}
                {{ form_end(peopleSearchForm) }}
            {% endif %}
        </div>
        -->
        <table class="table table-striped">
            <thead>
            <tr>
                <th>
                    Nr
                </th>
                <th>
                    {% trans %}event.name{% endtrans %}
                </th>
                <th>
                    {% trans %}event.description{% endtrans %}
                </th>
                <th>
                    {% trans %}event.price{% endtrans %}
                </th>
                <th>
                    {% trans %}event.date{% endtrans %}
                </th>
                <th>
                    {% trans %}event.place{% endtrans %}
                </th>
                <th>
                    &nbsp; (Bearbeiten/Löschen)
                </th>
            </tr>
            </thead>
            <tbody>
            {% for event in events %}
                <tr>
                    <td>
                        {{ loop.index}}
                    </td>
                    <td>
                        <a href="{{ path('detailEvent', {'uid': event.getId}) }}">{{ event.getName}}</a>
                    </td>
                    <td>
                        {{event.getDescription}}
                    </td>
                    <td>
                        {{event.getPrice}}
                    </td>
                    <td>
                        {{event.getDate_from}} - {{event.getDate_to}}
                    </td>
                    <td>
                        {{event.getPlace}}
                    </td>
                    <td>
                        {% include  'includes/removeUserButtonWithConfirmDialog.html.twig' with {"user":event} %}

                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}