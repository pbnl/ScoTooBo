# This file is auto-generated during the composer install
parameters:
    env(database_host): 127.0.0.1
    env(database_port): 3306
    env(database_name): scotoobo
    env(database_user): scotoobo
    env(database_password): secret
    env(ldap_base_dn): dc=pbnl,dc=de
    env(ldap_bind_dn): cn=admin,dc=pbnl,dc=de
    env(ldap_bind_password): admin
    env(ldap_server): 127.0.0.1
    env(ldap_port): 389
    env(ldap_tls): false
    env(mail_transport): smtp
    env(mail_host): 127.0.0.1
    env(mail_user): null
    env(mail_password): null
    env(mail_port): 465
    env(mail_encryption): tls
    env(secret): null
    env(recaptcha_secret): gSiteSecret
    env(recaptcha_globalcode): gGlobalCode
    env(staemme):  "[\"Ambronen\",\"Hagen von Tronje\",\"Anduril\", \"Dragon\", \"Caradhras\",\"Ynys Avilion\"]"
    env(sql_pdo): "pdo_sqlite"
    env(sqlite_path): "%kernel.project_dir%/var/data/data.sqlite"

    secret: "%env(secret)%"
    recaptcha.secret: "%env(recaptcha_secret)%"
    recaptcha.globalcode: "%env(recaptcha_globalcode)%"

    staemme: "%env(staemme)%"

    mail_port: '%env(mail_port)%'
    mail_encryption: tls
    mail_auth_mode: login